export class Point {
    row: number;
    col: number;
    pointChar: string;

    constructor(row: number, col: number, pointChar) {
        this.row = row;
        this.col = col;
        this.pointChar = pointChar;
    }
}

